{
  "name": "mcm",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "refresh": "node scripts/refresh.js",
    "dev": "vite frontend",
    "build": "node scripts/refresh.js && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
